<?php namespace App;

use Illuminate\Database\Eloquent\Model;

class ActTerm extends Model {

	//
    protected $fillable = [ 'term_id' ];
    
    //protected $table = 'act_terms';
    //protected $fillable = ['account_id', 'term_id'];
}